{
  "name": "cutsense-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CutSense backend render worker — manifest-driven AI talking-head editing pipeline.",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "worker": "tsx src/cli.ts",
    "dry-run": "tsx src/cli.ts --local"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@supabase/supabase-js": "^2.47.10",
    "groq-sdk": "^0.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
